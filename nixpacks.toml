[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput"
]

[start]
cmd = "gunicorn sistema_prenatal.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
